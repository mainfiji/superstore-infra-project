## Задание 1. Настройка VLAN на коммутаторах даркстора

**Что было сделано:**
- На коммутаторах SW1-Darkstore, SW2-Darkstore, SW3-Darkstore созданы VLAN 30 (Logistics), VLAN 40 (Storage), VLAN 60 (DS-Servers).
- Пользовательские порты переведены в режим access с соответствующими VLAN.
- Все соединения между коммутаторами и маршрутизатором R-Darkstore настроены как trunk-порты с разрешёнными VLAN 30,40,60.
- Клиентским устройствам временно назначены статические IP-адреса из соответствующих подсетей (например, PCL-1 получил 10.10.3.1/24) для проверки связности до настройки DHCP.

**Результаты проверки:**
Связность между устройствами в пределах одного VLAN проверена с помощью `ping`. Все пинги успешны, что подтверждает корректную работу VLAN и trunk-портов.

| Проверяемая пара | VLAN | Скриншот |
|------------------|------|----------|
| PCL-1 → PCL-2 | VLAN 30 (Logistics) | [`ping_PCL-1_to_PCL-2.png`](task1_vlan/ping_PCL-1_to_PCL-2.png) |
| PCL-3 → PCL-4 | VLAN 30 (Logistics) | [`ping_PCL-3_to_PCL-4.png`](task1_vlan/ping_PCL-3_to_PCL-4.png) |
| PCL-4 → PCL-3 (обратный) | VLAN 30 (Logistics) | [`ping_PCL-4_to_PCL-3.png`](task1_vlan/ping_PCL-4_to_PCL-3.png) |
| PCS-1 → PCS-2 | VLAN 40 (Storage) | [`ping_PCS-1_to_PCS-2.png`](task1_vlan/ping_PCS-1_to_PCS-2.png) |
| Portal → FS | VLAN 60 (DS-Servers) | [`ping_Portal_to_FS.png`](task1_vlan/ping_Portal_to_FS.png) |

**Вывод:** Настройка VLAN и trunk-соединений выполнена корректно, устройства внутри одной VLAN видят друг друга.

## Задание 2. Настройка маршрутизации на R-Darkstore

**Что было сделано:**
- На маршрутизаторе R-Darkstore созданы субинтерфейсы (sub-interfaces) для маршрутизации между VLAN:
  - `Ethernet0/0.30` — VLAN 30 (Logistics) с IP 10.10.3.1/24
  - `Ethernet0/0.40` — VLAN 40 (Storage) с IP 10.10.4.1/24
  - `Ethernet0/1.60` — VLAN 60 (DS-Servers) с IP 10.10.6.1/24
- Каждый субинтерфейс выполняет роль шлюза по умолчанию для своей подсети.
- Проверена доступность шлюзов с устройств из соответствующих VLAN, а также межсетевое взаимодействие между VLAN 30 и 40.

**Результаты проверки:**

**1. Доступность шлюзов по умолчанию:**

| Устройство | VLAN | Шлюз | Результат | Скриншот |
|------------|------|------|-----------|----------|
| PCL-1 | 30 (Logistics) | 10.10.3.1 | Успешно (TTL=255) | [`ping_PCL-1_to_gateway.png`](task2_routing/ping_PCL-1_to_gateway.png) |
| PCS-1 | 40 (Storage) | 10.10.4.1 | Успешно (TTL=255) | [`ping_PCS-1_to_gateway.png`](task2_routing/ping_PCS-1_to_gateway.png) |
| FS | 60 (DS-Servers) | 10.10.6.1 | Успешно (TTL=255) | [`ping_FS_to_gateway.png`](task2_routing/ping_FS_to_gateway.png) |
| Portal | 60 (DS-Servers) | 10.10.6.1 | Успешно (TTL=255) | [`ping_Portal_to_gateway.png`](task2_routing/ping_Portal_to_gateway.png) |

> **TTL=255** в ответах от шлюза говорит о том, что шлюз (маршрутизатор) отвечает сам, без дополнительных пересылок.

**2. Межсетевое взаимодействие между VLAN:**

| Проверяемая пара | Из VLAN | В VLAN | Результат | Скриншот |
|------------------|---------|--------|-----------|----------|
| PCL-1 → PCS-2 | 30 (Logistics) | 40 (Storage) | Успешно (TTL=63) | [`ping_PCL-1_to_PCS-2.png`](task2_routing/ping_PCL-1_to_PCS-2.png) |
| PCL-3 → PCS-3 | 30 (Logistics) | 40 (Storage) | Успешно (TTL=63) | [`ping_PCL-3_to_PCS-3.png`](task2_routing/ping_PCL-3_to_PCS-3.png) |
| PCL-4 → PCS-1 | 30 (Logistics) | 40 (Storage) | Успешно (TTL=63) | [`ping_PCL-4_to_PCS-1.png`](task2_routing/ping_PCL-4_to_PCS-1.png) |

> **TTL=63** — это признак того, что пакет прошёл через маршрутизатор (R-Darkstore), который уменьшил TTL на 1 (с 64 до 63). Это подтверждает, что межсетевая маршрутизация работает корректно.

**Вывод:** Субинтерфейсы на R-Darkstore настроены правильно. Шлюзы доступны для всех VLAN, а маршрутизация между VLAN 30 и 40 функционирует штатно.

## Задание 3. Настройка DHCP на R-Darkstore

**Что было сделано:**
- На маршрутизаторе R-Darkstore созданы DHCP-пулы для подсетей:
  - VLAN 30 (Logistics): пул с диапазоном адресов для 10.10.3.0/24
  - VLAN 40 (Storage): пул с диапазоном адресов для 10.10.4.0/24
- Ранее назначенные статические адреса на клиентских устройствах были удалены (или сброшены через `ip dhcp`).
- Клиенты автоматически получили IP-адреса из соответствующих пулов.

**Результаты проверки:**

| Устройство | VLAN | Полученный IP | Шлюз | Результат | Скриншот |
|------------|------|---------------|------|-----------|----------|
| PCL-3 | 30 (Logistics) | 10.10.3.5/24 | 10.10.3.1 | Успешно (DORA) | [`ip_dhcp_PCL-3.png`](task3_dhcp/ip_dhcp_PCL-3.png) |
| PCS-1 | 40 (Storage) | 10.10.4.6/24 | 10.10.4.1 | Успешно (DORA) | [`ip_dhcp_PCS-1.png`](task3_dhcp/ip_dhcp_PCS-1.png) |

**Процесс получения IP (DORA):**
- В выводе видна последовательность:
  - **D**iscover → **O**ffer → **R**equest → **A**ck (DORA)
  - Клиент успешно получил адрес, шлюз и маску подсети.

> **Примечание:** В процессе видно, что первый запрос не нашёл DHCP-сервер (отображено `Can't find dhcp server`), но при повторном запросе (после настройки пулов) адрес был успешно получен. Это нормальное поведение — DHCP-сервер не был доступен на момент первого запроса.

**Проверка связности:**
- После получения адреса клиенты успешно пингуют свои шлюзы и другие устройства.
- PCL-3 пингует 10.10.4.8 (устройство из другой VLAN), что подтверждает маршрутизацию.

**Вывод:** DHCP-сервер на R-Darkstore настроен корректно. Клиенты в VLAN 30 и VLAN 40 автоматически получают IP-адреса из правильных пулов и имеют сетевую связность.

## Задание 4. Настройка выхода в интернет (даркстор)

**Что было сделано:**
- На маршрутизаторе R-Darkstore интерфейс Ethernet0/2 настроен с IP-адресом 55.55.55.105/30.
- На маршрутизаторе провайдера ISP интерфейс Ethernet0/0 (или соответствующий) настроен с IP-адресом 55.55.55.106/30.
- На R-Darkstore добавлен маршрут по умолчанию: `ip route 0.0.0.0 0.0.0.0 55.55.55.106` – весь трафик, не попадающий под локальные маршруты, направляется к провайдеру.

**Результаты проверки:**

**1. Проверка связности с провайдером:**

| Откуда | Куда | Результат | Скриншот |
|--------|------|-----------|----------|
| R-Darkstore (55.55.55.105) | ISP (55.55.55.106) | Успешно (!!!!!) | [`ping_R-Darkstore_to_ISP.png`](task4_internet/ping_R-Darkstore_to_ISP.png) |

Пинг выполнен с указанием интерфейса-источника `e0/2`, что подтверждает использование правильного публичного адреса.

**2. Таблица маршрутизации R-Darkstore:**

На скриншоте видна запись:
- `S* 0.0.0.0/0 [1/0] via 55.55.55.106` – маршрут по умолчанию установлен и активен.
- Также присутствуют прямые подключения к локальным VLAN (10.10.3.0/24, 10.10.4.0/24, 10.10.6.0/24) и к подсети провайдера (55.55.55.104/30).

Скриншот: [`show_ip_route_R-Darkstore.png`](task4_internet/show_ip_route_R-Darkstore.png)

**Вывод:** Интерфейс стыка с провайдером настроен корректно, маршрут по умолчанию добавлен. Связность с ISP установлена, что позволяет в дальнейшем организовать NAT для доступа локальных сетей в интернет.

## Задание 5. Настройка тестового «интернет-сервера»

**Что было сделано:**
- На маршрутизаторе ISP создан дополнительный интерфейс `Ethernet0/2` с IP-адресом `1.1.1.2/24`.
- К этому интерфейсу подключено тестовое устройство (VPC) с адресом `1.1.1.1` (имитация сервера в интернете).
- Это позволяет проверить, что NAT и маршрутизация работают до внешнего мира.

**Результаты проверки:**

1. **Проверка связности с ISP:**
   - Пинг с ISP до R-Darkstore (55.55.55.105) — успешно.
   - Пинг с ISP до самого себя (55.55.55.106) — успешно (это проверка интерфейса).
   - Пинг с ISP до тестового сервера (1.1.1.1) — успешно, что подтверждает связность с «интернетом».

| Проверка | Скриншот |
|----------|----------|
| Пинги с ISP на разные адреса | [`ping_ISP_various.png`](task5_test_server/ping_ISP_various.png) |

2. **Таблица маршрутизации ISP:**
   - Видны подключенные сети: `1.1.1.0/24` (тестовый сервер) и `55.55.55.104/30` (стык с R-Darkstore).
   - Маршрута по умолчанию нет, так как ISP выступает в роли транзитного узла.

| Проверка | Скриншот |
|----------|----------|
| `show ip route` на ISP | [`show_ip_route_ISP.png`](task5_test_server/show_ip_route_ISP.png) |

**Вывод:** Тестовый сервер в интернете (1.1.1.1) доступен с ISP, а ISP видит R-Darkstore. Это создаёт основу для проверки NAT в следующем задании.
